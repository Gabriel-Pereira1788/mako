name: Build iOS

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/ios-build.yml'
      - 'mako-react-native/example/ios/**'
      - 'mako-react-native/example/Gemfile'
      - 'mako-react-native/example/Gemfile.lock'
      - 'mako-react-native/**/nitrogen/generated/shared/**'
      - 'mako-react-native/**/nitrogen/generated/ios/**'
      - 'mako-react-native/cpp/**'
      - 'mako-react-native/ios/**'
      - 'mako-react-native/**/Podfile.lock'
      - 'mako-react-native/**/bun.lock'
      - 'mako-react-native/**/*.podspec'
      - 'mako-react-native/**/react-native.config.js'
      - 'mako-react-native/**/nitro.json'
  pull_request:
    paths:
      - '.github/workflows/ios-build.yml'
      - 'mako-react-native/example/ios/**'
      - 'mako-react-native/example/Gemfile'
      - 'mako-react-native/example/Gemfile.lock'
      - 'mako-react-native/**/nitrogen/generated/shared/**'
      - 'mako-react-native/**/nitrogen/generated/ios/**'
      - 'mako-react-native/cpp/**'
      - 'mako-react-native/ios/**'
      - 'mako-react-native/**/Podfile.lock'
      - 'mako-react-native/**/bun.lock'
      - 'mako-react-native/**/*.podspec'
      - 'mako-react-native/**/react-native.config.js'
      - 'mako-react-native/**/nitro.json'
  workflow_dispatch:

env:
  USE_CCACHE: 1

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build iOS Example App
    runs-on: macOS-15
    defaults:
      run:
        working-directory: mako-react-native
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 16.4

      - name: Install dependencies (bun)
        run: bun install

      - name: Setup Ruby (bundle)
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: mako-react-native/example/ios

      - name: Install xcpretty
        run: gem install xcpretty

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: |
            ~/.cocoapods/repos
            mako-react-native/example/ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('mako-react-native/example/ios/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install Pods
        working-directory: mako-react-native/example/ios
        run: pod install

      - name: Build App
        working-directory: mako-react-native/example/ios
        run: |
          set -o pipefail && xcodebuild \
            CC=clang CPLUSPLUS=clang++ LD=clang LDPLUSPLUS=clang++ \
            -derivedDataPath build -UseModernBuildSystem=YES \
            -workspace NitroTraceExample.xcworkspace \
            -scheme NitroTraceExample \
            -sdk iphonesimulator \
            -configuration Debug \
            -destination 'platform=iOS Simulator,name=iPhone 16' \
            build \
            CODE_SIGNING_ALLOWED=NO | xcpretty
